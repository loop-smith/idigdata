# rig-vault env template · vault references resolved at dev/build time via `rig-vault env-resolve`
# generated: 2026-05-17T02:18:21.714Z
# source: .env.local
#
# Format: KEY=vault:<credential-id>  (resolved via vault canonical · F-008 18-field row)
# To produce a runtime .env from this template:
#   rig-vault env-resolve --from=.env.template --eval | source /dev/stdin
# Or run wrapped:
#   eval $(rig-vault env-resolve --from=.env.template --eval) && <your command>

# vault row idigdata.NEXT_PUBLIC_SUPABASE_URL (per-project)
NEXT_PUBLIC_SUPABASE_URL=vault:idigdata-next-public-supabase-url
# vault row idigdata.NEXT_PUBLIC_SUPABASE_ANON_KEY (per-project)
NEXT_PUBLIC_SUPABASE_ANON_KEY=vault:idigdata-next-public-supabase-anon-key
# vault row idigdata.NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION (per-project)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=vault:idigdata-next-public-google-site-verification
# vault row idigdata.NEXT_PUBLIC_BING_SITE_VERIFICATION (per-project)
NEXT_PUBLIC_BING_SITE_VERIFICATION=vault:idigdata-next-public-bing-site-verification
# Optional non-vault defaults. Fill or vault-map when needed.
WEBSITE_ALLOWED_ORIGINS=
NEXT_PUBLIC_WEBSITE_EVENT_INGEST_URL=
NEXT_PUBLIC_TRACK_PREVIEW_TRAFFIC=
# vault row idigdata.IDIGDATA_APP_SUPABASE_URL (per-project)
IDIGDATA_APP_SUPABASE_URL=vault:idigdata-idigdata-app-supabase-url
# vault row idigdata.IDIGDATA_APP_SUPABASE_ANON_KEY (per-project)
IDIGDATA_APP_SUPABASE_ANON_KEY=vault:idigdata-idigdata-app-supabase-anon-key
# vault row idigdata.IDIGDATA_APP_SUPABASE_SERVICE_ROLE_KEY (per-project)
IDIGDATA_APP_SUPABASE_SERVICE_ROLE_KEY=vault:idigdata-idigdata-app-supabase-service-role-key
# vault row idigdata.RESEND_API_KEY (per-project)
RESEND_API_KEY=vault:idigdata-resend-api-key
# vault row idigdata.EMAIL_NOTIFY_TO (per-project)
EMAIL_NOTIFY_TO=vault:idigdata-email-notify-to
EMAIL_NOTIFY_FROM=
