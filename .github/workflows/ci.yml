name: CI
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  ci-aggregate:
    name: ci-aggregate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "ci-aggregate present — real steps earn their way in"
