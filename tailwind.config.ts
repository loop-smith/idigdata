// LEGACY — Tailwind 4 migration 2026-04-21.
// Tailwind 4 uses CSS-first config via `@theme` and `@utility` in app/globals.css.
// This file is no longer read (no `@config` directive in CSS imports it).
// Safe to delete whenever convenient.
export {};
